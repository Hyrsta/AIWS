# 2026-04-10 AIWS5.2 清洗版数据集与 SAM3D 运行技术记录

**作者**: Weld  
**日期**: 2026-04-10  
**状态**: 已落地（可复现）

---

## 1. 文档目的与范围

本文给出 AIWS5.2 到 SAM3D 的正式工程基线，覆盖：

- 正式数据集定义（唯一口径）
- 服务器部署与依赖恢复
- RTX A6000 上 `flash_attn` 启用方案
- 批处理脚本技术规格
- 4 卡并行运行手册

本文只保留最终采用方案，不再展开已废弃的中间数据叙事。

---

## 2. 环境基线

### 2.1 本地工作区

- Workspace: `/Users/hyrsta/.openclaw/workspaces/welding-algorithm`
- 关键脚本：
  - `scripts/build_aiws52_usable_view.py`
  - `scripts/generate_aiws52_instance_masks.py`
  - `scripts/run_sam3d_aiws52_batch.py`

### 2.2 远端服务器（RXL）

- 项目根目录：`/ssd1/rxl/zhankaiming/AIWS`
- SAM3D 仓库：`/ssd1/rxl/zhankaiming/AIWS/repos/sam-3d-objects`
- Cadrille 仓库：`/ssd1/rxl/zhankaiming/AIWS/repos/cadrille`
- 数据集目录：`/ssd1/rxl/zhankaiming/AIWS/data/aiws5.2-usable-materialized`
- 批处理脚本：`/ssd1/rxl/zhankaiming/AIWS/scripts/run_sam3d_aiws52_batch.py`
- 结果根目录：`/ssd1/rxl/zhankaiming/AIWS/outputs`
- Conda 环境：`/home/rxl/anaconda3/envs/sam3d-objects`

### 2.3 本次正式运行根目录

- `/ssd1/rxl/zhankaiming/AIWS/outputs/sam3d-aiws52-clean-mesh-stl-20260410-193527`

---

## 3. 正式数据集定义（唯一口径）

### 3.1 正式数据集

- 名称：`aiws5.2-usable-materialized`
- 性质：清洗后的可运行数据视图（非破坏性、符号链接组织）
- 标注真值来源：`isat_annotations`

### 3.2 目录结构

- 顶层子集：`V1 / V2 / NEW`
- 每个子集按工件类别组织（如 `cover_plate`、`square_tube`、`h_beam`、`channel_steel`、`bellmouth`）
- 每个工件目录包含：
  - `images/`
  - `annotations/`
  - `depth_png/` 或 `depth_exr/`（按子集类型）

### 3.3 数据统计（`metadata/summary.json`）

- 正式任务总数：**1418**
- `V1`: 593
- `V2`: 524
- `NEW`: 301
- 多实例图像：23
- 多标签图像：0
- 未标注图像：1

### 3.4 叙事规则

后续技术文档、实验报告、方法说明统一采用以下表达：

- “本实验基于 `aiws5.2-usable-materialized` 清洗版数据集”
- “正式重建任务数为 1418”

不再引入中间版本路径与拆分历史。

---

## 4. 批处理脚本技术规格

脚本：`scripts/run_sam3d_aiws52_batch.py`

### 4.1 输入参数（关键）

- `--dataset-root`：数据集根目录
- `--dataset-layout`：`subset`（本项目正式配置）
- `--repo-root`：SAM3D 仓库目录
- `--output-root`：当前 shard 输出目录
- `--resume`：跳过已存在 `mesh.glb` + `mesh.stl` 的任务
- `--num-shards` / `--shard-index`：并行分片参数
- `--exclude-stems-file`：可选排除名单

### 4.2 单任务数据流

每个任务包含：

1. 读取 RGB 图像
2. 读取 annotation JSON
3. 从 polygon 栅格化得到二值 mask
4. 调用 `Inference(image, mask, seed)`
5. 导出 `mesh.glb` 与 `mesh.stl`
6. 写入实例级 `meta.json` 与全局 `results.jsonl`

### 4.3 分片策略

任务按 `global_index % num_shards == shard_index` 分配。

在 4 卡模式下：

- shard 0: `% 4 == 0`
- shard 1: `% 4 == 1`
- shard 2: `% 4 == 2`
- shard 3: `% 4 == 3`

保证：

- 不重复
- 不漏跑
- 可独立追踪每个 shard

### 4.4 产物与指标

每个 shard 产物：

- `manifest.csv`
- `results.jsonl`
- `summary.json`
- 每实例输出目录（含 `mesh.glb`、`mesh.stl`、`meta.json`）

关键指标：

- `duration_sec`
- `instances_per_hour`
- `sec_per_megapixel`
- `mask_pixels` / `mask_fraction`
- `peak_memory_allocated_mb` / `peak_memory_reserved_mb`
- `gpu_name`
- `num_shards` / `shard_index`

### 4.5 失败与恢复语义

- 失败任务会写入 `meta.json`（含错误类型、错误信息、traceback）
- 中间临时文件（`mesh.partial.*`）失败后自动清理
- `--resume` 可直接断点续跑

---

## 5. 服务器部署与依赖恢复

### 5.1 仓库恢复策略

在中国网络环境下，为规避直接拉取不稳定，采用：

1. 本地克隆官方仓库
2. 本地校验完整性
3. 同步到 RXL

当前仓库 commit：

- `81a82373a3a7f4cbb00bd5b32aaf6b4d0f659ddd`

### 5.2 运行时资源恢复

SAM3D 能稳定运行依赖以下资源齐全：

- `checkpoints/hf/pipeline.yaml`
- SAM3D checkpoints
- MoGe 本地权重
- DINOv2 本地 cache + checkpoint

关键路径：

- checkpoints: `/ssd1/rxl/zhankaiming/AIWS/repos/sam-3d-objects/checkpoints/hf`
- MoGe: `/ssd1/rxl/zhankaiming/AIWS/models/moge-vitl/model-real.pt`
- DINO cache: `/home/rxl/.cache/torch/hub/facebookresearch_dinov2_main`
- DINO checkpoint: `/home/rxl/.cache/torch/hub/checkpoints/dinov2_vitl14_reg4_pretrain.pth`

### 5.3 路径迁移修复

统一迁移到 `AIWS/` 根目录后，完成两项修复：

- batch runner 同时加入 `repo_root` 与 `repo_root/notebook` 到 `sys.path`
- `checkpoints/hf` 下旧符号链接重定向到新的 `AIWS/backups/...` 路径

---

## 6. `flash_attn` 在 RTX A6000 上的启用

### 6.1 问题一：预编译 wheel 二进制不兼容

- 预编译 wheel 依赖更高 glibc
- 服务器环境不满足

处理：服务器本地源码编译 `flash_attn`。

### 6.2 问题二：默认后端选择不会覆盖到 A6000

即使 import 成功，仓库默认后端选择也不会自动切到 A6000。

处理：显式设置环境变量：

- `ATTN_BACKEND=flash_attn`
- `SPARSE_ATTN_BACKEND=flash_attn`

### 6.3 最终状态

正式运行中 dense/sparse attention 均使用 `flash_attn`，日志可见后端信息。

---

## 7. 运行手册（Runbook）

### 7.1 启动单个 shard（示例）

```bash
ATTN_BACKEND=flash_attn \
SPARSE_ATTN_BACKEND=flash_attn \
CUDA_VISIBLE_DEVICES=0 \
/home/rxl/anaconda3/envs/sam3d-objects/bin/python -u \
/ssd1/rxl/zhankaiming/AIWS/scripts/run_sam3d_aiws52_batch.py \
  --dataset-root /ssd1/rxl/zhankaiming/AIWS/data/aiws5.2-usable-materialized \
  --dataset-layout subset \
  --repo-root /ssd1/rxl/zhankaiming/AIWS/repos/sam-3d-objects \
  --output-root /ssd1/rxl/zhankaiming/AIWS/outputs/sam3d-aiws52-clean-mesh-stl-20260410-193527/shard-0 \
  --resume \
  --num-shards 4 \
  --shard-index 0
```

### 7.2 监控 4 个 shard 汇总

```bash
for s in /ssd1/rxl/zhankaiming/AIWS/outputs/sam3d-aiws52-clean-mesh-stl-20260410-193527/shard-*; do
  echo "--- ${s} ---"
  jq '{completed_ok, failed, skipped, processed, total_tasks_in_shard, eta_sec}' "${s}/summary.json"
done
```

### 7.3 断点续跑

保留原参数并继续带 `--resume` 重启相应 shard 即可。

---

## 8. 当日可确认结果

1. 正式数据口径固定为 `aiws5.2-usable-materialized`
2. SAM3D 服务器环境恢复到可持续运行状态
3. RTX A6000 上 `flash_attn` 已进入正式运行路径
4. 4 shard 并行任务已启动并持续产出
5. 输出结构、实例级元数据、失败追踪链路完整

---

## 9. 后续建议

1. 等当前运行结束后合并四个 shard 的 `results.jsonl`
2. 建立失败样本清单并按错误类型分组分析
3. 基于本技术记录撰写论文方法与实验设置（保持单一正式口径）
4. 将本 runbook 固化到项目 SOP，后续按同一模板复现实验
