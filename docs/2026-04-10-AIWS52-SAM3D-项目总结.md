# 2026-04-10 AIWS5.2 与 SAM3D 项目总结

## 一、今日结论

今天已经把 AIWS5.2 与 SAM3D 的正式实验路线整理清楚，并落实为可运行的工程状态。

当前项目的标准方案如下：

- 正式数据集：`aiws5.2-usable-materialized`
- 正式项目根目录：`/ssd1/rxl/zhankaiming/AIWS`
- 正式输出格式：`mesh.glb + mesh.stl`
- 正式运行方式：4 张 RTX A6000 上的 4 shard 并行

为了保持后续写作与实验叙述清晰，本文只保留这条正式方案，不再引入已经废弃的中间数据版本。

---

## 二、数据侧成果

AIWS5.2 数据已经整理为适合正式实验的清洗版 materialized 数据集：

- 路径：`/ssd1/rxl/zhankaiming/AIWS/data/aiws5.2-usable-materialized`

其特点是：

- 按 `V1 / V2 / NEW` 组织
- 按工件类别组织
- 面向正式实验只保留清洗后的有效样本
- 后续文档中可直接把每个任务理解为一个正式重建样本

当前正式统计：

- 总任务数：**1418**
- 多实例图像：**23**
- 多标签图像：**0**
- 未标注图像：**1**

这使得后续实验写法可以保持简单：

- **1418 个样本**
- **1418 个正式重建任务**

---

## 三、环境侧成果

在 `RXL` 服务器上，SAM3D 已完成可持续使用的恢复与整理：

- SAM3D 仓库：`/ssd1/rxl/zhankaiming/AIWS/repos/sam-3d-objects`
- Cadrille 仓库：`/ssd1/rxl/zhankaiming/AIWS/repos/cadrille`
- Conda 环境：`/home/rxl/anaconda3/envs/sam3d-objects`

由于服务器位于中国网络环境，GitHub 与 Hugging Face 访问不稳定，因此采用了：

- 本地克隆与同步仓库
- 本地准备并同步外部依赖
- 从服务器备份恢复 checkpoint
- 本地 DINO cache 与本地 MoGe 路径引用

同时，项目相关文件已经统一收拢到：

- `/ssd1/rxl/zhankaiming/AIWS`

目录结构更适合后续长期维护。

---

## 四、`flash_attn` 结果

`flash_attn` 的问题已经解决到可用状态：

1. 通过源码编译解决了预编译 wheel 与服务器 glibc 的兼容性问题
2. 通过环境变量显式指定 backend，解决了 RTX A6000 不会自动启用 `flash_attn` 的问题

当前正式运行中：

- dense attention 使用 `flash_attn`
- sparse attention 使用 `flash_attn`

这意味着当前 4 卡运行已经建立在正确的 attention backend 之上。

---

## 五、批处理脚本成果

当前正式批处理脚本：

- `scripts/run_sam3d_aiws52_batch.py`

已经具备以下能力：

- 直接读取 `aiws5.2-usable-materialized`
- 运行时从 annotation polygon 栅格化 mask
- 输出 `mesh.glb` 与 `mesh.stl`
- 记录 `manifest.csv`、`results.jsonl`、`summary.json`、`meta.json`
- 支持 `--resume`
- 支持 4 shard 并行
- 记录实例级运行指标
- 自动把 DINO 请求重定向到本地 cache

另外，在项目统一移动到 `AIWS/` 根目录后，脚本与 checkpoint 路径问题也已经修复完成。

---

## 六、正式运行状态

当前正式运行目录：

- `/ssd1/rxl/zhankaiming/AIWS/outputs/sam3d-aiws52-clean-mesh-stl-20260410-193527`

运行方式：

- 4 张 RTX A6000
- 4 个 worker
- 4 个 shard
- 总任务数：**1418**

截至本文修订时：

- 正式运行已经启动成功
- 多个 shard 已开始稳定输出成功样本
- 运行日志、结果文件与每实例输出目录都已正常生成

---

## 七、今天最重要的成果

今天最重要的成果可以概括为五点：

1. 明确了 AIWS5.2 的**正式数据版本**，后续统一采用清洗版 `aiws5.2-usable-materialized`
2. 让 SAM3D 在 `RXL` 上恢复到可重复运行状态
3. 让 RTX A6000 真正跑上 `flash_attn`
4. 把项目资产统一整理进 `AIWS/` 根目录
5. 正式启动了面向 **1418** 个样本的 4 卡并行重建实验

---

## 八、后续建议

接下来建议继续做三件事：

1. 等待当前正式运行结束
2. 汇总 4 个 shard 的结果文件
3. 单独分析失败样本并整理实验报告

如果后面继续写论文、报告或项目周报，建议统一基于本文与技术记录版本展开，不再回到中途废弃的数据叙事。