# 2026-04-10 AIWS5.2 与 SAM3D 项目总结

## 1. 最终结论

今天已把 AIWS5.2 到 SAM3D 的正式工程路径固定为一条可复现方案：

- 正式数据集：`aiws5.2-usable-materialized`
- 项目根目录：`/ssd1/rxl/zhankaiming/AIWS`
- 输出格式：`mesh.glb + mesh.stl`
- 运行方式：4 张 RTX A6000，4 shard 并行

后续报告统一按该口径叙述，不再引入中间版本历史。

---

## 2. 数据侧结果

正式数据统计（`metadata/summary.json`）：

- 总任务：**1418**
- `V1`: 593
- `V2`: 524
- `NEW`: 301
- 多实例：23
- 多标签：0
- 未标注：1

可直接用于实验写作的表达：

- “1418 个清洗后的有效样本”
- “1418 个正式重建任务”

---

## 3. 环境侧结果

`RXL` 上 SAM3D 已恢复为可持续运行状态：

- SAM3D 仓库：`/ssd1/rxl/zhankaiming/AIWS/repos/sam-3d-objects`
- Conda 环境：`/home/rxl/anaconda3/envs/sam3d-objects`
- 运行资源（checkpoint / MoGe / DINO cache）已补齐
- 路径迁移到 `AIWS/` 后的链接与导入问题已修复

---

## 4. 性能路径结果（`flash_attn`）

`flash_attn` 已进入正式运行路径：

1. 通过源码编译解决 wheel 与系统兼容性问题
2. 通过环境变量显式指定后端，覆盖 A6000 默认不自动启用的问题

当前正式运行中 dense/sparse 均使用 `flash_attn`。

---

## 5. 批处理能力结果

正式批处理脚本：`scripts/sam3d_aiws52_batch.py`

已具备：

- `subset` 布局直接遍历
- 运行时 polygon→mask
- 双格式导出（GLB + STL）
- `manifest.csv` / `results.jsonl` / `summary.json` / `meta.json`
- `--resume` 断点续跑
- `--num-shards` + `--shard-index` 并行分片
- 实例级性能与显存指标记录

---

## 6. 运行状态

正式运行目录：

- `/ssd1/rxl/zhankaiming/AIWS/outputs/sam3d-aiws52-clean-mesh-stl-20260410-193527`

当前状态：

- 4 shard 已启动
- 持续产出成功样本
- 汇总文件与实例输出目录正常生成

---

## 7. 今日核心产出

1. 固化了唯一正式数据口径（`aiws5.2-usable-materialized`）
2. 恢复并稳定了 `RXL` 上 SAM3D 工程环境
3. 让 A6000 真正跑上 `flash_attn`
4. 统一了 `AIWS/` 根目录下的项目资产布局
5. 启动了 1418 样本级别的正式 4 卡并行重建

---

## 8. 下一步建议

1. 等待本轮正式运行完成
2. 合并四个 shard 的 `results.jsonl`
3. 统计失败任务并按错误类型分析
4. 基于当前技术记录继续撰写方法与实验章节
