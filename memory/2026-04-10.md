# 2026-04-10

- Worked on SAM3D setup on server `RXL`.
- Existing repo at `/ssd1/rxl/zhankaiming/sam-3d-objects` was backed up to `/ssd1/rxl/zhankaiming/sam-3d-objects.backup-20260410-122006` before replacing it.
- Direct `git clone` from the server to GitHub timed out on port 443, so a clean clone was pulled locally from `https://github.com/facebookresearch/sam-3d-objects.git` and synced to the server over SSH instead.
- Fresh repo now lives again at `/ssd1/rxl/zhankaiming/sam-3d-objects` on commit `81a82373a3a7f4cbb00bd5b32aaf6b4d0f659ddd` (`main`).
- The existing conda env at `/home/rxl/anaconda3/envs/sam3d-objects` is usable and was kept. Import smoke tests for `Inference`, `torch`, `kaolin`, `gsplat`, `pytorch3d`, `open3d`, and related deps passed.
- `flash_attn` import fails with missing `GLIBC_2.32`, but the repo defaults to `sdpa` on the server’s RTX A6000 GPUs, so this is not the immediate blocker.
- `python demo.py` initially failed because `/ssd1/rxl/zhankaiming/sam-3d-objects/checkpoints/hf/pipeline.yaml` was missing.
- A full 12 GB checkpoint bundle was found in older backup `/ssd1/rxl/zhankaiming/sam-3d-objects.backup-20260410-114956/checkpoints/sam3d` and restored into the fresh repo as a runtime `checkpoints/hf` directory using symlinks plus a patched local `pipeline.yaml`.
- The server cannot reach `huggingface.co`, so public dependencies were prefetched locally and copied to `RXL` instead of downloading from the server.
- MoGe weights were staged on the server at `/ssd1/rxl/zhankaiming/models/moge-vitl/model-real.pt`, and `checkpoints/hf/pipeline.yaml` was patched to use that local file path.
- DINOv2 torch.hub cache plus `dinov2_vitl14_reg4_pretrain.pth` were copied into `/home/rxl/.cache/torch/hub/` on the server.
- After these fixes, `python demo.py` completed successfully on `RXL` and wrote `splat.ply` in `/ssd1/rxl/zhankaiming/sam-3d-objects`.
- Added a safe helper launcher on the server at `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` plus runtime notes at `/ssd1/rxl/zhankaiming/SAM3D_RUNTIME.md`.
- The launcher supports `SAM3D_DRY_RUN=1` and refuses to overwrite an existing `splat.ply` unless `SAM3D_OVERWRITE=1` is set.
- Added local skill routing in `AGENTS.md` for `agent-config`, `technical-writing`, `ai-humanizer`, `humanize-chinese`, and `proactivity` so future sessions know when to read them.
- Initialized `~/proactivity/` with baseline state files and started using it to track the long-running `flash_attn` build on `RXL`.
- Leonardo asked to initialize the new local skills; `ai-humanizer` and `humanize-chinese` got quick smoke tests and are runnable locally.
- The real `flash_attn` problem on `RXL` is binary compatibility, not CUDA support: prebuilt wheels require `GLIBC_2.32`, so fixes must use a true local source build.
- A forced local `flash_attn` source build was started on `RXL` targeting Ampere-only code generation (`sm80` path, suitable for the RTX A6000 / `sm86`) after stopping setup from silently fetching incompatible wheels again.
- At the last live check before compaction, the `flash_attn` build was still progressing on `RXL`, roughly 24/72 CUDA object files complete.
- Workspace commit `429852c` records the local skill routing and proactivity initialization.
- Leonardo corrected the storage location for agent operating state: `proactivity/` and `self-improving/` should live inside this workspace, not under `~/`.
- Copied the existing proactive state into workspace-local `proactivity/` without deleting the home-directory copy, so the fix stayed reversible.
- Initialized workspace-local `self-improving/` with `memory.md`, `corrections.md`, `index.md`, `heartbeat-state.md`, and `projects/welding-algorithm.md`.
- Logged the correction into `self-improving/` as a reusable rule: keep workspace-local skill state inside the current workspace.
- The forced local `flash_attn` source build on `RXL` completed successfully, and `flash_attn 2.8.3` now imports cleanly in `/home/rxl/anaconda3/envs/sam3d-objects`.
- The first launcher rerun failed because `run_sam3d_demo.sh` used `set -u` around `conda activate`, which tripped the env's `activate-binutils_linux-64.sh` script on `ADDR2LINE`.
- Patched `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` to temporarily disable nounset during `source conda.sh` and `conda activate`, then reran the launcher successfully.
- `SAM3D_OVERWRITE=1 bash /ssd1/rxl/zhankaiming/run_sam3d_demo.sh` completed successfully after the patch and again produced `splat.ply` on `RXL`.
- The obsolete home-directory proactive state was moved to Trash at `/Users/hyrsta/.Trash/proactivity-20260410-1456` after the workspace-local copy was in place.

- Even after the `flash_attn` import fix, the end-to-end SAM3D sanity run still selected `sdpa` at runtime rather than `flash-attn`, so importability is fixed but runtime backend selection is still a separate follow-up.
- Workspace commit `5d4fd3e` records the `flash_attn` fix verification, launcher patch, and successful SAM3D sanity check.
- Added a reusable ops note under `self-improving/domains/ops.md`: avoid `set -u` during `conda activate` on this machine, and prefer Trash over permanent deletion for local agent-state cleanup.
- The reason SAM3D still used `sdpa` after the import fix was repo logic, not a broken install: `sam3d_objects/pipeline/inference_pipeline.py` only auto-selects `flash_attn` for A100, H100, and H200 GPUs.
- Verified on `RXL` that the RTX A6000 can run SAM3D successfully with real `flash_attn` when `ATTN_BACKEND=flash_attn` and `SPARSE_ATTN_BACKEND=flash_attn` are set.
- Updated `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` so its default behavior now exports those backend env vars, and confirmed a no-extra-env rerun completed successfully with logs showing `flash_attn` for both dense and sparse attention.
- Leonardo decided not to patch the original SAM3D repo further for now and asked to start the experiment on the full welding dataset in `aiws5.2-usable-split-materialized.zip`.
- The split-materialized AIWS5.2 view contains 1441 annotated image files and 1475 total object instances across train and val; multi-object images are handled as one SAM3D run per annotated instance.
- The dataset zip was already present on `RXL` and was unzipped there into `/ssd1/rxl/zhankaiming/aiws5.2-usable-split-materialized` before launching the batch run.
- Added workspace script `scripts/run_sam3d_aiws52_batch.py` to batch SAM3D over the dataset with resumable outputs, per-instance metadata, and per-run manifests/results files.
- The first batch smoke test exposed a separate infrastructure issue: `torch.hub` tried to reach GitHub again for `facebookresearch/dinov2` even though the local hub cache was already populated.
- Fixed that in the batch runner without patching the main repo by redirecting `torch.hub.load("facebookresearch/dinov2", ..., source="github")` to the local cache path `/home/rxl/.cache/torch/hub/facebookresearch_dinov2_main` with `source="local"`.
- Started the full resumable run on `RXL` at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-full-20260410-153416/`; it writes `manifest.csv`, `results.jsonl`, `summary.json`, and per-instance `splat.ply` outputs under split/subset/workpiece/stem folders.
- The full run was confirmed live after launch: task 1 (`train/NEW/cover_plate/NEW-G140-1__obj01`) and task 2 (`train/NEW/cover_plate/NEW-G140-10__obj01`) completed successfully with `flash_attn` active.
- Leonardo then corrected the rollout plan: we should not proceed straight to the full run without first locking down the right saved metrics and considering parallelism across the 4 RTX A6000 GPUs on `RXL`.
- The premature full run at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-full-20260410-153416/` was terminated cleanly after 20 successful instances, preserving the partial outputs for inspection instead of deleting them.
- `scripts/run_sam3d_aiws52_batch.py` was upgraded to save more useful experiment metrics: model init time, per-instance duration, instances/hour, seconds per megapixel, mask size/fraction, peak GPU allocated/reserved memory, GPU identity, and shard metadata.
- The runner now supports deterministic sharding for multi-GPU execution through `--num-shards` and `--shard-index`.
- A fresh 1-instance smoke test with the upgraded runner succeeded on GPU 2 (`CUDA_VISIBLE_DEVICES=2`, `--num-shards 4`, `--shard-index 2`) and saved the expected metrics in `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-smoke-metrics-20260410-1545/`.
- That smoke test recorded a realistic end-to-end instance time of about 23.5 seconds after model initialization and a peak GPU allocation of about 18.1 GiB on an RTX A6000.
- Leonardo then caught another important correctness issue: the intended saved artifact for this experiment is the mesh output, not the gaussian-splat export.
- Inspection of the SAM3D pipeline on `RXL` showed that the runner had been saving the wrong output branch: `output["gs"]` is the gaussian model with `save_ply`, while the mesh artifact is the postprocessed `output["glb"]` returned as a `trimesh.Trimesh` with `.export(...)`.
- Patched both the workspace runner and the deployed `/ssd1/rxl/zhankaiming/scripts/run_sam3d_aiws52_batch.py` copy to save `mesh.glb` instead of `splat.ply`, and to record `mesh_path`, `mesh_size_bytes`, and `artifact_format` in metadata.
- A fresh 1-instance smoke test with the corrected export path succeeded at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-smoke-mesh-20260410-1552/` on GPU 2, producing an 8.7 MB `mesh.glb` for `train/NEW/cover_plate/NEW-G140-11__obj01`.
- The corrected mesh-export smoke test took about 50.3 seconds end to end with a model init time of about 29.5 seconds and a peak GPU allocation of about 18.1 GiB.
# 2026-04-10

- Worked on SAM3D setup on server `RXL`.
- Existing repo at `/ssd1/rxl/zhankaiming/sam-3d-objects` was backed up to `/ssd1/rxl/zhankaiming/sam-3d-objects.backup-20260410-122006` before replacing it.
- Direct `git clone` from the server to GitHub timed out on port 443, so a clean clone was pulled locally from `https://github.com/facebookresearch/sam-3d-objects.git` and synced to the server over SSH instead.
- Fresh repo now lives again at `/ssd1/rxl/zhankaiming/sam-3d-objects` on commit `81a82373a3a7f4cbb00bd5b32aaf6b4d0f659ddd` (`main`).
- The existing conda env at `/home/rxl/anaconda3/envs/sam3d-objects` is usable and was kept. Import smoke tests for `Inference`, `torch`, `kaolin`, `gsplat`, `pytorch3d`, `open3d`, and related deps passed.
- `flash_attn` import fails with missing `GLIBC_2.32`, but the repo defaults to `sdpa` on the server’s RTX A6000 GPUs, so this is not the immediate blocker.
- `python demo.py` initially failed because `/ssd1/rxl/zhankaiming/sam-3d-objects/checkpoints/hf/pipeline.yaml` was missing.
- A full 12 GB checkpoint bundle was found in older backup `/ssd1/rxl/zhankaiming/sam-3d-objects.backup-20260410-114956/checkpoints/sam3d` and restored into the fresh repo as a runtime `checkpoints/hf` directory using symlinks plus a patched local `pipeline.yaml`.
- The server cannot reach `huggingface.co`, so public dependencies were prefetched locally and copied to `RXL` instead of downloading from the server.
- MoGe weights were staged on the server at `/ssd1/rxl/zhankaiming/models/moge-vitl/model-real.pt`, and `checkpoints/hf/pipeline.yaml` was patched to use that local file path.
- DINOv2 torch.hub cache plus `dinov2_vitl14_reg4_pretrain.pth` were copied into `/home/rxl/.cache/torch/hub/` on the server.
- After these fixes, `python demo.py` completed successfully on `RXL` and wrote `splat.ply` in `/ssd1/rxl/zhankaiming/sam-3d-objects`.
- Added a safe helper launcher on the server at `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` plus runtime notes at `/ssd1/rxl/zhankaiming/SAM3D_RUNTIME.md`.
- The launcher supports `SAM3D_DRY_RUN=1` and refuses to overwrite an existing `splat.ply` unless `SAM3D_OVERWRITE=1` is set.
- Added local skill routing in `AGENTS.md` for `agent-config`, `technical-writing`, `ai-humanizer`, `humanize-chinese`, and `proactivity` so future sessions know when to read them.
- Initialized `~/proactivity/` with baseline state files and started using it to track the long-running `flash_attn` build on `RXL`.
- Leonardo asked to initialize the new local skills; `ai-humanizer` and `humanize-chinese` got quick smoke tests and are runnable locally.
- The real `flash_attn` problem on `RXL` is binary compatibility, not CUDA support: prebuilt wheels require `GLIBC_2.32`, so fixes must use a true local source build.
- A forced local `flash_attn` source build was started on `RXL` targeting Ampere-only code generation (`sm80` path, suitable for the RTX A6000 / `sm86`) after stopping setup from silently fetching incompatible wheels again.
- At the last live check before compaction, the `flash_attn` build was still progressing on `RXL`, roughly 24/72 CUDA object files complete.
- Workspace commit `429852c` records the local skill routing and proactivity initialization.
- Leonardo corrected the storage location for agent operating state: `proactivity/` and `self-improving/` should live inside this workspace, not under `~/`.
- Copied the existing proactive state into workspace-local `proactivity/` without deleting the home-directory copy, so the fix stayed reversible.
- Initialized workspace-local `self-improving/` with `memory.md`, `corrections.md`, `index.md`, `heartbeat-state.md`, and `projects/welding-algorithm.md`.
- Logged the correction into `self-improving/` as a reusable rule: keep workspace-local skill state inside the current workspace.
- The forced local `flash_attn` source build on `RXL` completed successfully, and `flash_attn 2.8.3` now imports cleanly in `/home/rxl/anaconda3/envs/sam3d-objects`.
- The first launcher rerun failed because `run_sam3d_demo.sh` used `set -u` around `conda activate`, which tripped the env's `activate-binutils_linux-64.sh` script on `ADDR2LINE`.
- Patched `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` to temporarily disable nounset during `source conda.sh` and `conda activate`, then reran the launcher successfully.
- `SAM3D_OVERWRITE=1 bash /ssd1/rxl/zhankaiming/run_sam3d_demo.sh` completed successfully after the patch and again produced `splat.ply` on `RXL`.
- The obsolete home-directory proactive state was moved to Trash at `/Users/hyrsta/.Trash/proactivity-20260410-1456` after the workspace-local copy was in place.

- Even after the `flash_attn` import fix, the end-to-end SAM3D sanity run still selected `sdpa` at runtime rather than `flash-attn`, so importability is fixed but runtime backend selection is still a separate follow-up.
- Workspace commit `5d4fd3e` records the `flash_attn` fix verification, launcher patch, and successful SAM3D sanity check.
- Added a reusable ops note under `self-improving/domains/ops.md`: avoid `set -u` during `conda activate` on this machine, and prefer Trash over permanent deletion for local agent-state cleanup.
- The reason SAM3D still used `sdpa` after the import fix was repo logic, not a broken install: `sam3d_objects/pipeline/inference_pipeline.py` only auto-selects `flash_attn` for A100, H100, and H200 GPUs.
- Verified on `RXL` that the RTX A6000 can run SAM3D successfully with real `flash_attn` when `ATTN_BACKEND=flash_attn` and `SPARSE_ATTN_BACKEND=flash_attn` are set.
- Updated `/ssd1/rxl/zhankaiming/run_sam3d_demo.sh` so its default behavior now exports those backend env vars, and confirmed a no-extra-env rerun completed successfully with logs showing `flash_attn` for both dense and sparse attention.
- Leonardo decided not to patch the original SAM3D repo further for now and asked to start the experiment on the full welding dataset in `aiws5.2-usable-split-materialized.zip`.
- The split-materialized AIWS5.2 view contains 1441 annotated image files and 1475 total object instances across train and val; multi-object images are handled as one SAM3D run per annotated instance.
- The dataset zip was already present on `RXL` and was unzipped there into `/ssd1/rxl/zhankaiming/aiws5.2-usable-split-materialized` before launching the batch run.
- Added workspace script `scripts/run_sam3d_aiws52_batch.py` to batch SAM3D over the dataset with resumable outputs, per-instance metadata, and per-run manifests/results files.
- The first batch smoke test exposed a separate infrastructure issue: `torch.hub` tried to reach GitHub again for `facebookresearch/dinov2` even though the local hub cache was already populated.
- Fixed that in the batch runner without patching the main repo by redirecting `torch.hub.load("facebookresearch/dinov2", ..., source="github")` to the local cache path `/home/rxl/.cache/torch/hub/facebookresearch_dinov2_main` with `source="local"`.
- Started the full resumable run on `RXL` at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-full-20260410-153416/`; it writes `manifest.csv`, `results.jsonl`, `summary.json`, and per-instance `splat.ply` outputs under split/subset/workpiece/stem folders.
- The full run was confirmed live after launch: task 1 (`train/NEW/cover_plate/NEW-G140-1__obj01`) and task 2 (`train/NEW/cover_plate/NEW-G140-10__obj01`) completed successfully with `flash_attn` active.
- Leonardo then corrected the rollout plan: we should not proceed straight to the full run without first locking down the right saved metrics and considering parallelism across the 4 RTX A6000 GPUs on `RXL`.
- The premature full run at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-full-20260410-153416/` was terminated cleanly after 20 successful instances, preserving the partial outputs for inspection instead of deleting them.
- `scripts/run_sam3d_aiws52_batch.py` was upgraded to save more useful experiment metrics: model init time, per-instance duration, instances/hour, seconds per megapixel, mask size/fraction, peak GPU allocated/reserved memory, GPU identity, and shard metadata.
- The runner now supports deterministic sharding for multi-GPU execution through `--num-shards` and `--shard-index`.
- A fresh 1-instance smoke test with the upgraded runner succeeded on GPU 2 (`CUDA_VISIBLE_DEVICES=2`, `--num-shards 4`, `--shard-index 2`) and saved the expected metrics in `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-smoke-metrics-20260410-1545/`.
- That smoke test recorded a realistic end-to-end instance time of about 23.5 seconds after model initialization and a peak GPU allocation of about 18.1 GiB on an RTX A6000.
- Leonardo then caught another important correctness issue: the intended saved artifact for this experiment is the mesh output, not the gaussian-splat export.
- Inspection of the SAM3D pipeline on `RXL` showed that the runner had been saving the wrong output branch: `output["gs"]` is the gaussian model with `save_ply`, while the mesh artifact is the postprocessed `output["glb"]` returned as a `trimesh.Trimesh` with `.export(...)`.
- Patched both the workspace runner and the deployed `/ssd1/rxl/zhankaiming/scripts/run_sam3d_aiws52_batch.py` copy to save `mesh.glb` instead of `splat.ply`, and to record `mesh_path`, `mesh_size_bytes`, and `artifact_format` in metadata.
- A fresh 1-instance smoke test with the corrected export path succeeded at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-smoke-mesh-20260410-1552/` on GPU 2, producing an 8.7 MB `mesh.glb` for `train/NEW/cover_plate/NEW-G140-11__obj01`.
- The corrected mesh-export smoke test took about 50.3 seconds end to end with a model init time of about 29.5 seconds and a peak GPU allocation of about 18.1 GiB.
- Workspace commit `f68003e` records the switch from gaussian-splat export to mesh `mesh.glb` export in the SAM3D batch runner.
- Leonardo then clarified that the later Cadrille pipeline also needs `.stl`, so the batch runner was extended again to export both `mesh.glb` and `mesh.stl` for each instance.
- A fresh 1-instance smoke test with the dual-format export succeeded at `/ssd1/rxl/zhankaiming/outputs/sam3d-aiws52-smoke-mesh-stl-20260410-1613/` on GPU 2, producing `mesh.glb` (8.7 MB) and `mesh.stl` (22 MB) for `train/NEW/cover_plate/NEW-G140-11__obj01`.
- The dual-format smoke test took about 52.5 seconds end to end with a model init time of about 30.8 seconds and the same peak GPU allocation of about 18.1 GiB.
- With both GLB and STL saved, the full 1475-instance run is still comfortably within storage limits: the smoke-test sizes imply about 46.9 GB total output, far below the roughly 759 GB free on `/ssd1`.
- After the mesh correction, the full 4-GPU batch run was intentionally left paused; the next decision is whether to relaunch now with dual GLB+STL exports.
